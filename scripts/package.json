{
  "name": "resume-md2pdf-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Markdown resume -> themed HTML preview -> PDF via Puppeteer/Chromium",
  "scripts": {
    "setup": "npm install",
    "build": "node build_html.mjs",
    "pdf": "node render_pdf.mjs"
  },
  "dependencies": {
    "marked": "^12.0.2",
    "puppeteer": "^23.4.1"
  }
}
