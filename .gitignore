# 依赖（含 Chromium，约 47MB，由 `npm install` 重新生成）
node_modules/

# 运行时工作产物
.resume-theme.css
.brand-design.md
resume.html
resume.pdf
*.log

# 示例渲染产物（examples/frontend-resume/ 是 frontend-resume.md 的可再生产出，
# 其带前缀的文件名不在上面的默认模式内）
examples/frontend-resume/

# 临时/调试脚本
_*.mjs

# OS
.DS_Store
